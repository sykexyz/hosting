{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && pnpm -w run typecheck:libs",
    "start": "echo '@workspace/api-spec is a shared library, not a runnable service.' && exit 0"
  },
  "devDependencies": {
    "orval": "^8.20.0"
  }
}
